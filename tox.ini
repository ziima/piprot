[tox]
envlist = py35,py36,py37,py38

[testenv]
passenv =
    PYTHONWARNINGS
    CI TRAVIS TRAVIS_*
deps =
    coverage
    codecov
commands =
    coverage run --source=piprot --branch -m unittest discover
    codecov
